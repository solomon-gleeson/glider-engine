use bevy::prelude::*;

pub struct AssetsPlugin;

impl Plugin for AssetsPlugin {
    fn build(&self, _app: &mut App) {
        // Asset loading will be registered here later. (Review this @pizza)
    }
}
